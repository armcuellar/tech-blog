<div class="card-header d-flex justify-content-between">
    <a href="/comment/{{id}}">{{title}}</a>
    <div>
        Posted by
        {{user.username}}
        on
        {{format_date created_at}}
    </div>
</div>
<div class="card-body">
    {{content}}
</div>